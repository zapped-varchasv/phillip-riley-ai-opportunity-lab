'use strict';
const D=window.PRG_DATA,E=window.PRG_ENGINE;
const $=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD',maximumFractionDigits:0}).format(v);
const names={overview:'Overview',trials:'Trial studio',opportunities:'Opportunity register',business:'Business case',roadmap:'Delivery roadmap',research:'Research & evidence'};
const state={trial:'brief',scenario:0,result:null,approved:false,audit:[],paths:structuredClone(D.paths),inputs:{users:10,tasks:8,hourly:65,review:3,weeks:46},filter:'',team:'All teams',sort:'score'};
const header=(eyebrow,title,description,action='')=>`<div class="heading"><div><div class="eyebrow">${eyebrow}</div><h1>${title}</h1><p>${description}</p></div>${action}</div>`;
function toast(text){$('#toast').textContent=text;$('#toast').classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>$('#toast').classList.remove('show'),4500);}
function download(name,text,type='text/markdown'){const blob=new Blob([text],{type}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);}
function overview(){return header('RENEWABLE ENERGY. HUMAN CONNECTION.','A practical case for AI at PRG.','An evidence-led exploration of where automation could give recruiters more time for clients and candidates.', '<a class="button" href="#research">Explore the research ↗</a>')+`
 <section class="hero"><div><div class="eyebrow">RECOMMENDATION 01 / START WITH WHAT EXISTS</div><h2>Prove the value.<br>Then choose the technology.</h2><p>Review current tools, test three everyday workflows, and measure the time saved after human review.</p><a class="button green" href="#trials">Open the trial studio <span>↗</span></a></div><div class="hero-visual"><div class="ring" role="img" aria-label="Three synthetic workflow demonstrations"><div><strong>3</strong><small>WORKFLOW DEMOS</small></div></div><dl><dt>Built around</dt><dd>Renewable-energy recruitment</dd><dt>First decision</dt><dd>Use → buy → build</dd><dt>Designed for</dt><dd>A supervised 12-week discovery</dd></dl></div></section>
 <div class="stats"><div class="stat"><div class="stat-label">Opportunities to validate <span>↗</span></div><strong>16</strong><small>Ranked with a transparent rubric</small></div><div class="stat"><div class="stat-label">Controlled workflow demos <span>⚡</span></div><strong>03</strong><small>Draft, check, review, export</small></div><div class="stat"><div class="stat-label">Commercial paths <span>◇</span></div><strong>04</strong><small>Editable 1-year and 3-year costs</small></div><div class="stat"><div class="stat-label">Delivery horizon <span>→</span></div><strong>12 <span style="font-size:17px">weeks</span></strong><small>With ownership and handover gates</small></div></div>
 <div class="section-heading"><h2>From real work to a useful trial</h2><a href="#trials">Try all three workflows ↗</a></div>
 <div class="cards">${[
 ['01','brief','Brief to advert','Turn an approved BESS or wind role brief into an advert draft, with missing information made visible.','Recruitment delivery','↗'],
 ['02','care','Candidate care','Prepare a factual status update. Test what happens when contact permission has not been recorded.','Candidate experience','◎'],
 ['03','pulse','Client pulse','Turn a synthetic hiring pipeline into a concise progress brief with a clear blocker and next action.','Client service','▥']
 ].map(([n,type,title,desc,tag,icon])=>`<a class="card" href="#trials/${type}"><span class="num">TRIAL ${n}</span><span class="icon-box">${icon}</span><h3>${title}</h3><p>${desc}</p><div class="card-bottom"><span>${tag}</span><b>↗</b></div></a>`).join('')}</div>
 <div class="bottom-grid"><section class="panel"><h2>The proposed operating loop</h2><div class="flow"><span>Approved<br>input</span><b>→</b><span>Draft &<br>checks</span><b>→</b><span>Recruiter<br>review</span><b>→</b><span>Measured<br>outcome</span></div><p>A person owns every release. No candidate ranking, hiring decisions or live record changes.</p></section><section class="panel"><h2>A hypothesis, ready to challenge</h2><p>Start with existing capabilities. JobAdder advertises AI drafting, and ROI-AI offers recruitment automation. Confirm what is licensed, configured and used before funding a new build.</p><a class="button small spaced" href="#business">Challenge the business case →</a></section></div>
 <div class="notice spaced">This is an independent interview prototype, not a PRG deployment. Trial outputs use deterministic templates and rules, not a live AI model. Business-case figures are editable assumptions, not measured savings.</div>`;}
const trialMeta={brief:['Brief to advert','Test an approved role brief and a missing-information scenario.'],care:['Candidate care','Test a status update and a contact-permission hold.'],pulse:['Client pulse','Test pipeline reporting with a factual next action.']};
function field(label,key,value,type='text',readonly=false){return `<div class="field"><label for="f-${key}">${label}</label>${type==='textarea'?`<textarea id="f-${key}" data-field="${key}">${esc(value)}</textarea>`:type==='permission'?`<select id="f-${key}" data-field="${key}"><option value="yes" ${value==='yes'?'selected':''}>Recorded for this example</option><option value="no" ${value==='no'?'selected':''}>Not recorded — hold</option></select>`:`<input id="f-${key}" data-field="${key}" value="${esc(value)}" type="${type}" ${type==='number'?'min="0" max="9999" step="1"':''} ${readonly?'readonly':''}>`}</div>`;}
function trials(){const s=D.scenarios[state.trial][state.scenario];return header('THREE SMALL, CONTROLLED EXPERIMENTS','Trial studio','Run a synthetic scenario, inspect its evidence, then review the result. Nothing is sent to a client, candidate or AI provider.')+`
 <div class="tabs" role="tablist" aria-label="Trial selection">${Object.entries(trialMeta).map(([k,[name]])=>`<button class="tab ${state.trial===k?'active':''}" role="tab" aria-selected="${state.trial===k}" data-trial="${k}">${name}</button>`).join('')}</div>
 <div class="trial-grid"><section class="panel"><div class="step-label">01 / APPROVED INPUT</div><h2>${trialMeta[state.trial][0]}</h2><p>${trialMeta[state.trial][1]} Edit the fields to see how the output changes.</p><div class="field spaced"><label for="scenario">Synthetic scenario</label><select id="scenario">${D.scenarios[state.trial].map((v,i)=>`<option value="${i}" ${i===state.scenario?'selected':''}>${v.label}</option>`).join('')}</select></div>
 <form id="trial-form">${state.trial==='brief'?field('Role title','role',s.role)+`<div class="field-row">${field('Sector','sector',s.sector)}${field('Location','location',s.location)}</div>`+field('Work pattern','pattern',s.pattern)+field('Salary','salary',s.salary)+field('Role purpose','summary',s.summary,'textarea')+field('Experience to discuss','skills',s.skills,'textarea'):state.trial==='care'?field('Synthetic candidate reference','candidate',s.candidate,'text',true)+field('Role','role',s.role)+field('Verified status','status',s.status)+field('Next action','next',s.next,'textarea')+field('Contact permission','permission',s.permission,'permission'):field('Fictional client','client',s.client)+field('Hiring programme','role',s.role)+`<div class="field-row">${field('Sourced','sourced',s.sourced,'number')}${field('Screened','screened',s.screened,'number')}${field('Submitted','submitted',s.submitted,'number')}${field('At interview stage','interviews',s.interviews,'number')}</div>`+field('Current blocker','blocker',s.blocker,'textarea')+field('Next action','next',s.next,'textarea')}
 <div class="source">Source: ${s.source} · fictional fixture · local browser only</div><button class="button primary" type="submit">Run demo checks & draft ↗</button></form></section>
 <section class="panel" id="trial-output" aria-live="polite">${outputHTML()}</section></div><div class="notice spaced">Demonstration scope: deterministic text assembly and validation. In a real trial, compare these steps with approved existing tools or an approved model using the same inputs, quality checks and timing sheet.</div>`;}
function outputHTML(){const r=state.result;if(!r)return `<div class="step-label">02 / EVIDENCE & HUMAN REVIEW</div><h2>A draft you can inspect</h2><div class="empty"><span>⌁</span><h3>Choose a scenario. Run the trial.</h3><p>The result will show its source fields, missing information and release controls.</p></div><div class="spaced"><h3>What this trial measures</h3><p class="spaced">Time to a reviewed output, factual accuracy, correction rate and staff willingness to use it.</p><div class="source">No trial results or savings are claimed in this prototype.</div></div>`;
return `<div class="step-label">02 / EVIDENCE & HUMAN REVIEW</div><div class="section-heading"><h2>${r.title}</h2><span class="badge ${r.blocked?'amber':''}">${r.blocked?'Needs clarification':state.approved?'Reviewed demo':'Review required'}</span></div><div class="source">${esc(r.source)} · template v1.0 · ${esc(state.runTime)}</div><div class="output">${esc(r.text)}</div><div class="review ${r.blocked?'blocked':''}"><b class="small">${r.blocked?'Release blocked':'Before release'}</b><ul>${r.flags.map(f=>`<li>${esc(f)}</li>`).join('')}</ul></div>
<details class="spaced"><summary>View source evidence (${r.evidence.length} fields)</summary>${r.evidence.map(([k,v])=>`<div class="evidence-row"><b>${esc(k)}</b><span>${esc(v)||'Not provided'}</span></div>`).join('')}</details>
<label class="checks"><input type="checkbox" id="review-check" ${r.blocked?'disabled':''} ${state.approved?'checked disabled':''}><span>I have checked the source fields, factual accuracy and wording. This approves only the synthetic demo output.</span></label>
<div class="row-actions"><button class="button primary" id="approve" disabled>${state.approved?'Reviewed ✓':'Mark reviewed'}</button><button class="button" id="export-trial" ${!state.approved?'disabled':''}>Export reviewed brief ↓</button></div><div class="audit"><h3>Session activity</h3><ol>${state.audit.map(v=>`<li>${esc(v)}</li>`).join('')}</ol><div class="source">In-memory activity only; resets on refresh. Not a production audit system.</div></div>`;}
function bindTrials(){document.querySelectorAll('[data-trial]').forEach(b=>b.onclick=()=>{state.trial=b.dataset.trial;state.scenario=0;state.result=null;state.approved=false;location.hash='trials/'+state.trial;render();});$('#scenario').onchange=e=>{state.scenario=+e.target.value;state.result=null;state.approved=false;render();};$('#trial-form').oninput=()=>{if(state.result){state.result=null;state.approved=false;$('#trial-output').innerHTML=outputHTML();}};$('#trial-form').onsubmit=e=>{e.preventDefault();const s={...D.scenarios[state.trial][state.scenario]};document.querySelectorAll('[data-field]').forEach(el=>s[el.dataset.field]=el.value.trim());state.result=E.trial(state.trial,s);state.approved=false;state.runTime=new Date().toLocaleTimeString('en-AU',{hour:'2-digit',minute:'2-digit'});state.audit.push(`${state.runTime}: ${state.result.title}; ${state.result.blocked?'held for clarification':'draft ready for review'}.`);$('#trial-output').innerHTML=outputHTML();bindReview();toast(state.result.blocked?'A validation check blocked release.':'Draft ready. Inspect the evidence before review.');};bindReview();}
function bindReview(){if(!state.result)return;$('#review-check').onchange=e=>$('#approve').disabled=!e.target.checked;$('#approve').onclick=()=>{if(state.result.blocked||!$('#review-check').checked)return;state.approved=true;state.audit.push(`${new Date().toLocaleTimeString('en-AU',{hour:'2-digit',minute:'2-digit'})}: User marked the synthetic output reviewed.`);$('#trial-output').innerHTML=outputHTML();bindReview();toast('Reviewed. Your demo brief is ready to export.');};$('#export-trial').onclick=()=>{if(!state.approved||state.result.blocked)return;download('prg-reviewed-demo.md',`# ${state.result.title}\n\nIndependent interview prototype by Varchasv Gupta. Synthetic data only.\nGenerated by deterministic template v1.0; not a live model.\n\n${state.result.text}\n\n## Evidence\n${state.result.evidence.map(([k,v])=>`- ${k}: ${v}`).join('\n')}\n\nSource: ${state.result.source}\n\n## Review history\n${state.audit.map(v=>'- '+v).join('\n')}\n\nNo message was sent and no operational record changed.`);toast('Reviewed demo downloaded. Nothing was sent.');};}
function opportunityRows(){return D.opportunities.filter(o=>(state.team==='All teams'||o.team===state.team)&&`${o.title} ${o.why} ${o.team}`.toLowerCase().includes(state.filter.toLowerCase())).sort((a,b)=>state.sort==='risk'?a.risk-b.risk||E.score(b)-E.score(a):E.score(b)-E.score(a)).map(o=>`<tr><td><small>${o.id}</small><b>${o.title}</b></td><td>${o.team}</td><td><div class="score"><b>${E.score(o)}</b><span class="score-bar"><i style="width:${E.score(o)}%"></i></span></div></td><td><span class="badge ${o.risk>=4?'amber':'neutral'}">${['','Low','Moderate','Elevated','High','Very high'][o.risk]}</span></td><td>${o.path}</td><td><button class="button small" data-detail="${o.id}" aria-expanded="false">Why +</button></td></tr><tr id="detail-${o.id}" class="detail-row" hidden><td colspan="6"><p><strong>Why it matters:</strong> ${o.why}</p><p><strong>Measure:</strong> ${o.measure}</p><p><strong>Rubric inputs:</strong> impact ${o.impact}/5 · feasibility ${o.feasibility}/5 · adoption ${o.adoption}/5 · risk ${o.risk}/5. All are provisional judgments.</p><p><strong>Discovery gate:</strong> confirm the process owner, current feature usage, baseline time and data permissions before trial selection.</p></td></tr>`).join('')||'<tr><td colspan="6">No opportunities match. Try another search or team.</td></tr>';}
function opportunities(){return header('BUSINESS VALUE BEFORE TECHNOLOGY','Opportunity register','Sixteen PRG-specific hypotheses, scored for a discovery conversation. These are proposed opportunities, not confirmed operational gaps.','<button class="button" id="export-register">Export register ↓</button>')+`<div class="notice">Priority = 35% impact + 25% feasibility + 20% likely adoption + 20% lower risk. Each dimension uses 1–5; total is scaled to 100. The scores are editable in the repository and must be recalibrated with staff.</div><div class="filterbar"><input id="search" aria-label="Search opportunities" placeholder="Search opportunity or workflow…" value="${esc(state.filter)}"><select id="team" aria-label="Filter by team">${['All teams',...new Set(D.opportunities.map(o=>o.team))].map(t=>`<option ${t===state.team?'selected':''}>${t}</option>`).join('')}</select><select id="sort" aria-label="Sort opportunities"><option value="score" ${state.sort==='score'?'selected':''}>Highest priority</option><option value="risk" ${state.sort==='risk'?'selected':''}>Lowest risk first</option></select></div><div class="table-wrap"><table><thead><tr><th>Opportunity</th><th>Team</th><th>Score / 100</th><th>Risk</th><th>First path to test</th><th>Reason</th></tr></thead><tbody id="opportunity-rows">${opportunityRows()}</tbody></table></div><p class="small muted spaced">Risk levels reflect proposed data and workflow exposure, not a legal assessment. A high priority score does not override approval or data-access requirements.</p>`;}
function bindOpportunities(){const refresh=()=>{$('#opportunity-rows').innerHTML=opportunityRows();bindDetails();};$('#search').oninput=e=>{state.filter=e.target.value;refresh();};$('#team').onchange=e=>{state.team=e.target.value;refresh();};$('#sort').onchange=e=>{state.sort=e.target.value;refresh();};bindDetails();$('#export-register').onclick=()=>{const cols=['id','title','team','score','impact','feasibility','adoption','risk','path','why','measure'];const rows=D.opportunities.map(o=>({...o,score:E.score(o)})).sort((a,b)=>b.score-a.score);download('prg-opportunities.csv',[cols.join(','),...rows.map(o=>cols.map(k=>'"'+String(o[k]).replaceAll('"','""')+'"').join(','))].join('\r\n'),'text/csv');};}
function bindDetails(){document.querySelectorAll('[data-detail]').forEach(b=>b.onclick=()=>{const r=$('#detail-'+b.dataset.detail);r.hidden=!r.hidden;b.textContent=r.hidden?'Why +':'Close −';b.setAttribute('aria-expanded',String(!r.hidden));});}
function range(label,key,min,max,step){return `<div class="field"><label class="range-label" for="input-${key}"><span>${label}</span><output id="value-${key}">${state.inputs[key]}</output></label><input type="range" id="input-${key}" data-input="${key}" min="${min}" max="${max}" step="${step}" value="${state.inputs[key]}"></div>`;}
function costCards(){return state.paths.map((p,i)=>{const c=E.businessCase(state.inputs,p);return `<section class="cost-card ${i===0?'selected':''}"><span class="badge ${i?'neutral':''}">${p.tag}</span><h3>${String.fromCharCode(65+i)}. ${p.name}</h3><p>${p.description}</p><div class="cost-value ${c.net1<0?'negative':'positive'}">${money(c.net1)}</div><div class="cost-label">Year-one net capacity value · illustrative AUD</div><div class="cost-mini"><div><b>${money(c.year1)}</b><small>Year-one total cost</small></div><div><b>${money(c.year3)}</b><small>Three-year total cost</small></div><div><b>${Math.round(c.hours)} h</b><small>Annual capacity released</small></div><div><b>${c.payback===null?'No payback':c.payback.toFixed(1)+' months'}</b><small>Upfront cost payback*</small></div></div><details><summary>Edit cost & adoption assumptions</summary><div class="field-row">${[['Setup AUD','setup'],['Training AUD','training'],['Monthly software AUD','monthly'],['Monthly support AUD','support'],['Adoption %','adoption'],['Gross minutes saved / task','minutes']].map(([label,key])=>`<div class="field"><label for="cost-${p.id}-${key}">${label}</label><input id="cost-${p.id}-${key}" type="number" min="0" max="${key==='adoption'?100:key==='minutes'?180:1000000}" step="1" data-path="${p.id}" data-cost="${key}" value="${p[key]}"></div>`).join('')}</div></details><details><summary>Value calculation & risk</summary><p>${esc(p.risk)}</p><p>Annual gross capacity value: ${money(c.benefit)}. Three-year net capacity value: ${money(c.net3)}. Year-one break-even capacity: ${c.breakEvenHours===null?'not defined at zero hourly value':Math.round(c.breakEvenHours)+' hours'}.</p></details></section>`;}).join('');}
function business(){return header('MAKE EVERY ASSUMPTION VISIBLE','The business case','Compare four ways to deliver the same workflow benefit. Adjust your assumptions and see where the economics change.','<button class="button" id="export-case">Export options paper ↓</button>')+`<div class="notice amber">Illustrative planning inputs, not vendor quotes or PRG financial data. Time released is capacity value; it becomes cash savings only if actual expenditure is removed. Do not add all four options together.</div><div class="cost-layout spaced"><aside class="panel cost-controls"><h2>Shared assumptions</h2><p class="small">A single comparable workflow, across the same team.</p>${range('Team members','users',0,40,1)}${range('Tasks / person / week','tasks',0,30,1)}${range('Loaded hourly value (AUD)','hourly',0,150,5)}${range('Review minutes / task','review',0,20,1)}${range('Working weeks / year','weeks',0,52,1)}<button class="button small" id="reset-case">Reset assumptions</button></aside><div class="cost-cards" id="cost-cards">${costCards()}</div></div><section class="panel spaced"><h2>How the model works</h2><div class="formula">Annual hours = people × tasks/week × working weeks × max(0, gross minutes saved − review minutes) ÷ 60 × adoption<br>Year-one cost = setup + training + 12 × (monthly software + monthly support)<br>Three-year cost = setup + training + 36 × (monthly software + monthly support)<br>Net capacity value = annual hours × hourly value − cost</div><p class="spaced">*Payback uses upfront costs divided by monthly capacity value after recurring costs. It assumes immediate steady-state use. Adoption ramp-up, financing, inflation, tax and revenue uplift are excluded. Existing licence costs are sunk for this incremental comparison; additional fees still need confirmation. Review time greater than savings yields zero benefit.</p></section>`;}
function bindCosts(){document.querySelectorAll('[data-path]').forEach(el=>el.onchange=()=>{const p=state.paths.find(p=>p.id===el.dataset.path),limit=el.dataset.cost==='adoption'?100:el.dataset.cost==='minutes'?180:1000000;p[el.dataset.cost]=Math.max(0,Math.min(limit,Number(el.value)||0));const openPaths=[...document.querySelectorAll('.cost-card')].map(c=>[...c.querySelectorAll('details')].map(d=>d.open));$('#cost-cards').innerHTML=costCards();document.querySelectorAll('.cost-card').forEach((c,i)=>c.querySelectorAll('details').forEach((d,j)=>d.open=openPaths[i][j]));bindCosts();});}
function bindBusiness(){document.querySelectorAll('[data-input]').forEach(el=>el.oninput=()=>{state.inputs[el.dataset.input]=+el.value;$('#value-'+el.dataset.input).value=el.value;$('#cost-cards').innerHTML=costCards();bindCosts();});bindCosts();$('#reset-case').onclick=()=>{state.paths=structuredClone(D.paths);state.inputs={users:10,tasks:8,hourly:65,review:3,weeks:46};render();};$('#export-case').onclick=()=>download('prg-options-paper.md',`# PRG illustrative options paper\n\nIndependent interview prototype. All costs are AUD planning assumptions, not vendor quotes. No PRG savings have been measured.\n\n## Shared inputs\n${Object.entries(state.inputs).map(([k,v])=>`- ${k}: ${v}`).join('\n')}\n\n${state.paths.map(p=>{const c=E.businessCase(state.inputs,p);return `## ${p.name}\n${p.description}\n\n${Object.entries(p).filter(([k])=>['setup','monthly','training','support','adoption','minutes'].includes(k)).map(([k,v])=>`- ${k}: ${v}`).join('\n')}\n- Year-one cost: ${money(c.year1)}\n- Three-year cost: ${money(c.year3)}\n- Annual hours released: ${c.hours.toFixed(1)}\n- Year-one net capacity value: ${money(c.net1)}\n- Three-year net capacity value: ${money(c.net3)}\n- Upfront payback: ${c.payback===null?'None':c.payback.toFixed(1)+' months'}\n- Risk: ${p.risk}\n`;}).join('\n')}\n## Interpretation\nValidate existing-tool capability first. Compare alternatives for the same workflow; do not sum their benefits. Capacity value is not cash savings. Payback assumes steady-state adoption from day one. No inflation, financing, tax, revenue uplift or adoption ramp included.\n\n## Formula\nHours = people * tasks/week * weeks * max(0, minutes saved - review minutes) / 60 * adoption. Costs include setup, training, subscriptions and support.\n`);}
function roadmap(){const milestones=[['01–02','Learn the work and audit the tools','Shadow recruiters and support teams. Map handovers, licences, configuration and actual usage. Review the existing assistants.','Gate: process owners confirm the map and data boundaries.'],['03–04','Prioritise and agree the trials','Score 10–20 opportunities with staff. Baseline two or three workflows and select small trials with the supervisor.','Gate: each trial has a hypothesis, owner, measurement sheet and stop rule.'],['05–07','Run controlled comparisons','Use approved synthetic or anonymised inputs. Compare current practice with assisted work, including correction and review time.','Gate: no critical factual errors or unauthorised release; evidence supports time savings.'],['08–09','Compare commercial paths','Check existing entitlements, test integrations in an approved environment and obtain comparable costs for the same scope.','Gate: one-year and three-year costs include setup, support and training.'],['10','Make the tailored-solution decision','Recommend a custom layer only if current tools cannot meet a proven need and there is a named maintenance owner.','Gate: documented use, buy or build recommendation with uncertainties.'],['11–12','Train, present and hand over','Deliver staff guidance, walkthroughs, an owner register, trial evidence and the 12-month roadmap.','Gate: a colleague can rerun the trial and explain how to stop it.']];return header('SMALL TRIALS. CLEAR DECISIONS.','A 12-week delivery roadmap','Designed for a supervised, part-time placement. The prototype starts a discovery conversation; final priorities depend on PRG’s workflow and access.')+`<div class="bottom-grid" style="margin-top:0"><section class="panel timeline">${milestones.map(([w,title,desc,gate])=>`<div class="milestone"><div class="week">WEEKS<br>${w}</div><div><h3>${title}</h3><p>${desc}</p><small>${gate}</small></div></div>`).join('')}</section><aside><section class="panel"><h2>Trial success criteria</h2><ul><li>At least 20 approved examples per trial, including missing and conflicting information.</li><li>At least 30% lower median end-to-end time, including review.</li><li>Zero critical invented facts or unauthorised releases in the evaluated set.</li><li>At least 80% of trial staff would use the workflow again.</li><li>A documented fallback if the tool fails.</li></ul><div class="notice">These are proposed acceptance thresholds. Twenty examples can identify obvious failures; they do not establish production reliability.</div></section><section class="panel spaced"><h2>Handover from day one</h2><ul><li>Named business and technical owners</li><li>Versioned prompts and synthetic examples</li><li>Test results and decision log</li><li>Approved data and access boundaries</li><li>Operating guide and support contacts</li><li>Failure, rollback and review schedule</li></ul></section></aside></div><div class="section-heading spaced"><h2>From the placement to adoption</h2></div><div class="cards">${[['First 90 days','Discover and validate','Finish controlled trials, compare options and secure an evidence-backed decision.'],['Months 3–6','Expand proven workflows','Train a small staff group, monitor quality and time, and fund only the validated gaps.'],['Months 6–12','Review and consolidate','Reassess usage, renewal costs, risk and ownership. Retire duplicate workflows where justified.']].map(([n,t,d])=>`<section class="card"><span class="num">${n}</span><h3>${t}</h3><p>${d}</p></section>`).join('')}</div>`;}
function research(){return header('WHAT IS KNOWN. WHAT NEEDS VALIDATION.','Research & evidence','Public sources reviewed on 25 September 2026. The supplied role brief informs the proposed project scope; it is not reproduced in this public repository.')+`<div class="research-list">${[
 ['Public company source','A specialist across the energy lifecycle','Phillip Riley describes renewable-energy recruitment spanning executive search, permanent recruitment and contract resourcing. That supports workflow trials across delivery and client service.','https://www.phillipriley.com.au/','Company overview'],
 ['Public company source','Sector-specific, not generic recruiting','The company lists energy storage, wind, solar and transmission among its sectors, with development, engineering, operations and executive specialisms. These inform the fictional scenarios.','https://www.phillipriley.com.au/industries-sectors/','Industries & sectors'],
 ['Vendor capability claim','Check the existing AI feature set','JobAdder advertises AI job-ad drafting, candidate summaries and record-update prompts. Public capability does not prove PRG’s entitlement, configuration, cost or adoption.','https://jobadder.com/ai-recruitment-software/','JobAdder AI capabilities'],
 ['Vendor capability claim','Avoid duplicate automation','ROI-AI describes its JobAdder integration and its role in Advanced Automations. Validate the actual contract and workflow before assuming a new connector is needed.','https://www.roi-ai.com/integrations/','ROI-AI integrations'],
 ['Public company source','Sales includes account development','A public PRG strategic-accounts role describes national account growth and cross-service planning. A source-backed account-preparation trial is therefore a reasonable hypothesis.','https://www.phillipriley.com.au/job/national-business-development-strategic-accounts-leader/','Public strategic-accounts role'],
 ['Public company source','Candidate information needs care','PRG’s public privacy policy describes candidate, client and administrative information handling. Use fictional records only. Saved work is stored in SQL; explicitly submitted assistant questions and selected drafts are sent to OpenAI.','https://www.phillipriley.com.au/privacy-policy/','Company privacy policy']
 ].map(([tag,title,desc,url,label])=>`<section class="panel"><span class="badge neutral">${tag}</span><h3>${title}</h3><p>${desc}</p><a href="${url}" target="_blank" rel="noreferrer">${label} ↗</a></section>`).join('')}</div><div class="bottom-grid"><section class="panel"><h2>Questions I would ask first</h2><ul><li>Where is the same information entered more than once?</li><li>Which licensed features are configured, and which do staff actually use?</li><li>What do the current assistants do well, and where do staff rework their output?</li><li>Which delays most affect candidate experience or client relationships?</li><li>Who can approve data access, own the trial and verify the value?</li></ul></section><section class="panel"><h2>Boundaries of this research</h2><p>Public pages show services and vendor capabilities, not PRG’s internal process. Workflow maps, opportunity scores, effort estimates and costs here are hypotheses. No employee interviews, operational access, vendor quotes or live pilots have occurred.</p><div class="notice spaced">The strongest next step is a discovery workshop and a baseline measurement—not a purchase or production deployment.</div></section></div>`;}
function render(){const [raw,sub]=location.hash.slice(1).split('/'),page=names[raw]?raw:'overview';if(page==='trials'&&trialMeta[sub]&&state.trial!==sub){state.trial=sub;state.scenario=0;state.result=null;state.approved=false;}$('#page-name').textContent=names[page];document.title=`${names[page]} · PRG Opportunity Lab`;document.querySelectorAll('[data-page]').forEach(a=>{a.classList.toggle('active',a.dataset.page===page);if(a.dataset.page===page)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current');});$('#main').innerHTML=({overview,trials,opportunities,business,roadmap,research}[page])();if(page==='trials')bindTrials();if(page==='opportunities')bindOpportunities();if(page==='business')bindBusiness();}
document.querySelector('.skip').onclick=e=>{e.preventDefault();document.querySelector('#main').focus();};
window.addEventListener('hashchange',()=>{state.result=null;state.approved=false;render();window.scrollTo(0,0);});render();


