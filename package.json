{
  "name": "phillip-riley-ai-opportunity-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Independent interview portfolio with synthetic recruitment workflow trials",
  "scripts": {"start": "node scripts/serve.cjs", "test": "node --test tests/*.test.cjs", "check": "node --check dist/app.js && node --check dist/engine.js && node --check dist/data.js"},
  "engines": {"node": ">=20"},
  "license": "MIT"
}
