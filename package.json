{
  "name": "phillip-riley-ai-opportunity-lab",
  "version": "2.0.0",
  "private": true,
  "description": "Independent interview portfolio with synthetic recruitment workflow trials",
  "scripts": {
    "start": "node --experimental-sqlite scripts/dev-server.mjs",
    "start:offline": "node scripts/serve.cjs",
    "build": "node scripts/build.mjs",
    "db:generate": "drizzle-kit generate",
    "test": "node --experimental-sqlite --test tests/*.test.cjs tests/*.test.mjs",
    "check": "node --check dist/app.js && node --check dist/workspace.js && node --check server/worker.mjs && node --check dist/engine.js && node --check dist/data.js && node --check scripts/dev-server.mjs && node --check scripts/build.mjs"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "drizzle-kit": "0.31.10",
    "esbuild": "0.25.5"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "license": "MIT"
}
